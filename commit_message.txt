Dev, PelleNybe/Corax CoLAB: [performance improvement]

💡 What: Wrapped `ForceGraph3D` in `RiskRadarPanel.tsx` in `React.memo()`.
🎯 Why: Prevents expensive Three.js graph re-renders caused by frequent parent state updates (like logs or defcon).
📊 Impact: Expected to reduce CPU usage and layout calculations on each log entry update.
🔬 Measurement: Verify by checking that `ForceGraph3D` doesn't re-render when `addLog` is called.
