💡 What: Added React.memo to Terrain in VolatilityMatrix.tsx and Wall in HoloOrderFlow.tsx
🎯 Why: High-frequency polling and state updates in parent components caused child Three.js components to re-render unnecessarily, leading to GPU strain and UI lag.
📊 Impact: Prevents expensive layout recalculations and reduces massive Three.js sub-tree updates, improving frame rates during polling.
🔬 Measurement: Verify smooth animations in VolatilityMatrix and HoloOrderFlow via React Profiler.
