Dev, PelleNybe/Corax CoLAB: [security improvement]

💡 What: Removed potential parameters leakage via explicit object creation rather than spreading arguments into order_placed socket emission.
🎯 Why: Prevents leaking sensitive authentication tokens (e.g., DASHBOARD_PASSWORD) if they were previously included in params during order submission.
📊 Impact: Expected to completely prevent leakage in the order execution logic.
🔬 Measurement: Verify order placed emissions in the UI WebSocket traffic for the absence of `params` containing `approval_token`.
