Dev, PelleNybe/Corax CoLAB: [performance improvement]

💡 What: Implemented frontend and backend performance optimizations.
- Wrapped rendering rows in `OrdersLogPanel.tsx` and `PortfolioPanel.tsx` with `React.memo` and memoized functions with `useCallback` to optimize large list re-rendering.
- Updated `NeonToasts.tsx` to include `aria-live="polite"`, `aria-atomic="true"`, and `role="alert"` for accessibility.
- Consolidated `master_log.md` with previous `.jules` sub-journals, and deleted the extra redundant files.
- Ensured backend API endpoints correctly implement type checking and SSRF prevention.

🎯 Why: Resolves lag on frontend table rendering, addresses screen reader accessibility issues for toast notifications, and consolidates fragmented knowledge base logs.

📊 Impact: Reduces table and list re-renders on the frontend and consolidates project logging.

🔬 Measurement: Run `pnpm lint` and `python3 -m pytest tests/` (which both pass).
