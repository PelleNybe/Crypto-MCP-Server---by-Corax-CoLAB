Dev, PelleNybe/Corax CoLAB: [performance improvement]

💡 What:
- Fixed a double fetch in RiskRadarPanel.tsx.
- Removed duplicate title attributes across App.tsx and OrderPanel.tsx.
- Verified hooks and MemoizedForceGraph3D usage across features.

🎯 Why: Resolves double fetching on component mount reducing rate limits hit, and addresses screen reader accessibility guidelines regarding duplicate titles on inputs.

📊 Impact: Reduces unnecessary API load by half on Risk Radar load. Clears up accessibility tests.

🔬 Measurement: Run `pnpm run test` in `gui/frontend` to ensure Dashboard still renders, and `pytest tests/` for backend.
