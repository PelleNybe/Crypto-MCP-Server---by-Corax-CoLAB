fix(backend): Convert arrow function to regular function for stmt.run callback

Fixed a minor consistency issue where one SQLite `stmt.run` callback used an arrow function instead of the standard `function (err)` convention, aligning it with other db/stmt.run callbacks in the codebase. Tests pass locally.
