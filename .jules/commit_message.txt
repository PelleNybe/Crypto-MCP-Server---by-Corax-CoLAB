Dev, PelleNybe/Corax CoLAB: [performance improvement] Multiple optimizations

💡 What:
- Changed `setInterval` polling for portfolio and ticker to recursive `setTimeout` to prevent API piling.
- Verified absence of redundant `React.memo` wrappers on `CanvasScene`.
- Verified `DASHBOARD_PASSWORD` doesn't leak via socket.io.
- Fixed an accessibility issue in `PredictiveGhosting.tsx` by adding `aria-label`.
- Logged fix in `master_log.md`

🎯 Why: To prevent API piling, enhance screen reader support, and guarantee security.

📊 Impact: Reduces CPU usage, network rate limiting, and enhances accessibility.

🔬 Measurement: Verified by clean test passes, observing network traffic, and React DevTools profiling.
